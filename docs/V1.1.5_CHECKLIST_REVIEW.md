# simMC Tool Set v1.1.5 清单复核

复核日期：2026-08-24  
权威清单：`simmc-tool-set_project/错误报告/V1.1.4 测试bug与改进意见需求列表.docx`  
基线：`fabric-mc1.21.8-tool-set-v1.1.4` / `a680ea8`  
当前分支：`fabric-mc1.21.8-tool-set-v1.1.5`  
功能提交：`e072c38`、`252b8a1`

## 对照规则

逐项检查当前 DOCX、源代码、单元测试、构建、JAR 审计和运行时证据。
源码和构建通过不等于目标服务器或未知 Xaero 版本已验收；未取得实机证据的条目保持未验证。

## 分配与边界

| 等级 | 负责人 | 文件边界 | 结果 |
| --- | --- | --- | --- |
| P4/P3/P1 | 本轮无条目 | 不分配 | 不适用 |
| P2.1 | 1 个 gpt-5.6-Sol-high 子代理 | `internal/simes/ArcaneCooldownParser.java`、对应奥术契约测试 | 已合并 |
| P0.1 第一阶段 | 1 个 gpt-5.6-Sol-high 子代理 | Xaero 兼容判定、实际 Mixin 插件、地图状态 UI 和 map smoke | 已合并 |
| 版本、整合、构建、清单复核 | 主线程 | 版本文件、合并、构建、JAR、证据 | 已完成本地复核 |

## 逐项闭环

| 清单项 | 源码/测试证据 | 构建/JAR 证据 | 当前判定 |
| --- | --- | --- | --- |
| P2.1 冷却条约 2 秒延迟 | 解析器恢复 Simes 1.4.0 的无尾锚 `matcher.find()`；按匹配区间保留全部残余；新增混合首包、多格式、多冷却、反例、下一帧状态和公共冷却独立测试 | `verifyUnitTests` 53/53；完整 Xaero 与无 Xaero构建均通过 | 源码和单测已覆盖；目标服短/长 CD 仍需实测，不提前宣称完全验收 |
| P0.1 多 Xaero 兼容，第一阶段 | 仅 World Map `1.39.13` + Minimap `25.2.16` 返回 `shouldLoadMap/shouldApplyMixins=true`；不兼容、缺失、未知、外置 SIMMC Map 和旧 experimental 设置均不初始化/不应用地图 Mixin；总控显示安全停用和其他模块继续运行 | `MapCompatibilitySmoke` 7/7；完整 Xaero `MapNativeSmoke` 通过；无 Xaero map implementation 按设计跳过；JAR 含两个地图 Mixin及实际注册插件 | 第一阶段源码/构建已覆盖；未知版本真实客户端启动仍需用户验收；第二阶段 ABI 适配尚未实现 |

## 构建证据

- 项目 JDK 21、Fabric Loader 0.17.3、Gradle 9.5.0。
- 完整 Xaero：`clean verifyUnitTests verifyMapCompatibility build -x test` 成功；53/53、MapCompatibilitySmoke 7/7、MapNativeSmoke 通过。
- 无 Xaero：`-Pxaero_lib_dir=.xaero-empty clean verifyUnitTests verifyMapCompatibility build -x test` 成功；53/53、MapCompatibilitySmoke 7/7，地图实现按设计跳过。
- 完整 Xaero 构建仍有既有 GuiMap remap 和 `renderOutsidePip` 警告；不将其误判为运行时兼容证明。

## 候选 JAR 与审计

- 候选：`release/simmc-tool-set-fabric-1.1.5-fabric-mc1.21.8.jar`
- 大小：1,348,744 bytes。
- SHA-256：`EFAB78BFC7803A21B30E39419A433CBF423B4DF61036FEDB8BC1F0FD185706C9`，相邻 `.jar.sha256` 一致。
- `fabric.mod.json` 版本为 `1.1.5-fabric`、环境为 `client`、单一客户端入口。
- JAR 470 个条目、无重复条目；包含两个地图 Mixin、实际注册的 `com...map.ToolSetMapMixinPlugin` 和 `META-INF/jars/jsoup-1.18.3.jar`。
- 未打包 Xaero/Minecraft/Fabric 类；未发现 `ArcaneStatusState`、market、balance、autoMessage 等禁用功能类。

## 尚未验证与发布边界

- 未启动或修改用户客户端 `I:\mc 起源服务器\versions\1.21.8-Fabric 0.17.3`。
- 目标服务器短 CD、长 CD、公共冷却和首包残余文本尚无新实机日志或截图；P2 保持运行时未验证。
- 未知 Xaero 组合尚无真实客户端启动截图/日志；第一阶段只证明门控和构建行为，不能宣称未知版本功能兼容。
- 第二阶段能力探测、ABI 指纹、World Map/Minimap 独立适配器和代表版本验证未实现。
- 未创建 `v1.1.5-fabric-mc1.21.8` 标签或线上 Release；旧分支、旧 JAR、校验文件和 Release 未覆盖。
