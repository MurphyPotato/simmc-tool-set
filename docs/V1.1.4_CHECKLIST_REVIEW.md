# simMC Tool Set v1.1.4 清单复核

复核日期：2026-08-23  
权威清单：`simmc-tool-set_project/错误报告/V1.1.3 测试bug与改进意见需求列表.docx`  
基线：v1.1.3 文档提交 `b9acb09`  
当前分支：`fabric-mc1.21.8-tool-set-v1.1.4`  
最新实现提交：`9f31f87`

## 对照规则

每一项分别核对源码、单元/静态测试、构建/JAR 和运行时证据。源码覆盖或构建成功不能替代 Minecraft 界面和目标服务器证据；上一版本的完成结论不自动继承。

## 分配与边界

| 等级 | 负责人 | 文件边界 | 结果 |
| --- | --- | --- | --- |
| P4 + P3 | 1 个 gpt-5.6-LUNA-MAX 子代理 | `ToolSetScreen.java` 和对应页面测试 | 已合入并由主线程复核 |
| P2 + P1 | 1 个 gpt-5.6-Sol-high 子代理 | `internal/simes/**`、`SimesBossBarMixin` 和奥术测试 | 已合入并由主线程复核 |
| 共享生命周期、版本、构建、审计 | 主线程 | `SimesFeatureController`、版本与文档 | 已完成源码和构建复核 |

## 逐项闭环

| 条目 | 源码/测试证据 | 构建/JAR 证据 | 当前判定 |
| --- | --- | --- | --- |
| P4.1 | 卷轴页旧长说明已替换为简短介绍；契约测试检查新文案 | 完整构建通过 | 已解决（静态证据） |
| P3.1 | 诊断日志正文使用独立裁剪区，底部按钮固定；重进页面回到最新日志，滚轮边界有契约测试 | 完整构建通过 | 源码覆盖，隔离客户端视觉未验证 |
| P3.2 | 地图页四个主要按钮共享同一宽度 | 完整构建通过 | 源码覆盖，宽/窄屏视觉未验证 |
| P2.1 | 第一个可解析冷却 Action Bar 包立即创建冷却状态；没有按“释放”消息猜测 CD；49 项单元测试覆盖首包状态 | 完整构建通过 | 源码覆盖但目标服未验证 |
| P1.1 | 清单标记为“未正确测试，暂不修复” | 无新增运行证据 | 未正确测试/暂不修复 |
| P1.2 | 未整体迁移发酵/厨具，现有实现保持不变 | 无新增运行证据 | 保持现状 |
| P1.3 | 删除 `ArcaneStatusState`；恢复冷却、吟唱/持续、公共冷却、BossBar 抑制/恢复；新增 `ManaHud`、经验包入口、三个功能开关和两种显示模式 | `fabric.mod.json` 为 client-only；Mixin 含单一 `SimesBossBarMixin`；Mana 类已打包 | 源码覆盖但目标服未验证 |
| P1.4 | 四 HUD 同屏预览，支持选择、拖动、50%-200% 独立缩放、10% 步进及重置；坐标和边界测试通过 | 完整构建通过 | 源码覆盖，隔离客户端交互未验证 |
| P0 | 当前清单无条目 | 不适用 | 无 |

## 构建证据

- 项目 JDK 21、Fabric Loader 0.17.3。
- 无 Xaero：`clean verifyUnitTests verifyMapCompatibility build -x test` 通过；49/49 单元测试，兼容场景 3/3，原生地图 smoke 按预期跳过。
- 完整 Xaero：同一任务通过；49/49 单元测试，`MapNativeSmoke` 通过，兼容场景 3/3。
- 完整构建仍输出既有 Xaero Mixin remap 警告；构建成功不证明真实 Xaero 页面兼容。
- 候选 JAR：`release/simmc-tool-set-fabric-1.1.4-fabric-mc1.21.8.jar`，1,348,895 bytes。
- SHA-256：`75A9A3CE3F1DC75B103EEF9B6C467B303F455F3BF4912D8FB746AE46F5844042`；相邻 `.jar.sha256` 已复算匹配，旧产物未覆盖。

## 静态审计与边界

- 版本元数据为 `1.1.4-fabric`，环境为 `client`。
- `jsoup 1.18.3` 内嵌；Xaero 类不打包。
- `ArcaneStatusState` 类已删除；`ManaHud` 和 `SimesBossBarMixin` 已打包。
- Simes 市场、估值、余额和自动消息仍不属于授权迁移范围；地图地块数据中的同名字段不属于 Simes 功能。
- 未创建 v1.1.4 标签或线上 Release。

## 运行时未验证

未启动、修改或复用用户客户端 `I:\mc 起源服务器\versions\1.21.8-Fabric 0.17.3`。本轮未取得目标服务器的奥术释放、短 CD、吟唱、持续、公共冷却或 Mana 日志/截图，也未完成隔离客户端的设置页、四 HUD 拖动、诊断滚动和地图按钮视觉验收。因此本版本只能称为候选产物，不能宣称完整发布。
