# v1.1.9-dev.20260915.1 验收记录

本文件是本次迭代的证据清单，不代表已完成交付。未填入通过证据的项目仍未通过。
服务器衰减参数、取整行为及真实材料累计量始终属于未验证的本地模型边界。

| 项目 | 必要证据 | 当前结果 |
|---|---|---|
| 原始材料表 | 首表 126 行与资源逐项一致，来源摘要 | 导入及 `--check` 通过；完整构建待最终验证 |
| 累计公式 | 独立常数、零投入、分批差分、负值不静默截断 | 首轮领域测试已添加，修订后待重跑 |
| 精度 | 未取整值用于杂质/溢出/目标约束，显示才四舍五入 | 修复中 |
| 非线性搜索 | 高 M 补料、禁用唯一来源、多人/当天 M 输入 | 修复中 |
| 无额外用料效率 | 固定单卷轴材料向量的最大可行重复次数，不使用 64 硬阈值 | 修复中 |
| 默认方案 | 多批次共享 M、总目标守恒、最终 M 排序后可行 | 修复中 |
| 搜索预算 | 三档、单调截止时间、保留最佳方案、取消不覆盖新结果 | 修复中 |
| 320 输入上限 | 主材料展示开关不改变实际输入限制 | 待完整回归 |
| 玩家/日期隔离 | UUID、北京时间跨日、重启恢复、保留历史 | 未完成 |
| 原子确认 | M 与历史同时写入，重复点击/重启幂等，失败不改内存 | 未完成 |
| 临时层 | 取消/换卷轴/重置不改正式 M，已保存行不再次计入 | 未完成 |
| 自动模式 | 默认启用、冻结结果、手动控件禁用 | 未完成 |
| 手动数量 | 数量警告、减少尾部方案后二次确认并均分、总数检查 | 未完成 |
| 重置/单方案保存 | 恢复未保存默认、已保存内容不回滚或重复记账 | 未完成 |
| 编辑 M | 使用记录入口、明确警告、必选确认框、审计旧值新值 | 未完成 |
| 使用记录 | 时间/模式/配方/材料/前后 M、跨日历史、滚动 | 未完成 |
| 预设 | 单个/组合、20 字符、改名/删除、重新模拟、失败二次确认 | 未完成 |
| 真实界面操作 | 两种窗口尺寸、编辑与确认、预设、Esc/父界面恢复 | 未完成 |
| 模块回归 | Mana、厨具、发酵桶及原有筛选/主材料开关 | 未完成 |
| 分发审计 | 全测试、隔离客户端、Gradle build、diff check、JAR/SHA-256 | 未完成 |
| 历史产物保留 | 旧版本及已有同名不完整候选可恢复 | 已有同名候选按哈希备份；最终交付待验证 |

## 已保存的旧候选

工作区 `release/archive/incomplete-20260915.1-73AFA13AA39E/` 保存原同名候选及
校验文件，SHA-256 为
`73AFA13AA39E3A25F209CEC54C39C0239DF0B69E3297CD11452113CC90975D7E`。
此候选不能视为本次计划完整交付包。
